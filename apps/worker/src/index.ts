// Background worker stub — BullMQ processors for transcription, newsletter, etc.

console.log("[aslico/worker] Worker stub ready. Queue processors will be added in Phase 1.")

// Keep process alive in dev
setInterval(() => {}, 60_000)
