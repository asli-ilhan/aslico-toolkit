# Local voice clone TTS for Self Therapy (Mac / Apple Silicon friendly)
# Zero-shot clone via Coqui XTTS v2 — runs only on your machine, no ElevenLabs.

httpx>=0.27.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
numpy>=1.24
soundfile>=0.12
# Coqui TTS (XTTS v2). First run downloads ~2GB model weights.
# Pin transformers<5 — v5 breaks XTTS imports.
coqui-tts[codec]>=0.27.0
transformers>=4.43,<5
torch>=2.2.0
torchaudio>=2.2.0
